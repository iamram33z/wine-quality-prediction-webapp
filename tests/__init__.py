"""
Initialization for the test package.
Sets up shared imports and configurations.
"""

import pytest
import logging
from unittest import mock

# Configure logging for test runs
logging.basicConfig(level=logging.INFO)
